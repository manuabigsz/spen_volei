<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base. As cores reais do app são definidas em Compose (Material 3).
         Este tema serve apenas como ponto de partida da Activity. -->
    <style name="Theme.SPenPlacar" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
